events {
    worker_connections 1024;
}

http {
    upstream api {
        server worker1:5215;
        server api:5215;
    }

    server{
        listen 80;
        location /api {
            proxy_pass http://api;
        }
    }


}